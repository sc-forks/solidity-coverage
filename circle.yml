machine:
  node:
    version: 8.9.1
dependencies:
  override:
    - rm -rf node_modules/
    - npm install
test:
  override:
    - npm run lint
    - npm run test-cov
  post:
    - bash <(curl -s https://codecov.io/bash)
